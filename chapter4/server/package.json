{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run build && node lib/server.js",
    "build": "babel src -d lib"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.17.1",
    "events": "^1.1.1",
    "express": "^4.15.2",
    "web-push": "^3.2.2"
  },
  "devDependencies": {
    "babel-cli": "^6.24.0"
  }
}